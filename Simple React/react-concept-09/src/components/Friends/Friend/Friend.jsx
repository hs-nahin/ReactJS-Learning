import React from 'react';

const Friend = (props) => {
    return (
        <>
            <div>
                <li> Close Friend : {props.name}</li>
            </div>
        </>
    );
};

export default Friend;
